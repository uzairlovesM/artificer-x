<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Section: Camera/media capture — CameraX photo/video capture. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Declared but not required: this app's camera features are an
         enhancement, not core functionality, so devices without a
         camera (rare, but e.g. some tablets) can still install it —
         CameraCaptureScreen checks PackageManager.FEATURE_CAMERA_ANY
         at runtime and hides camera entry points when absent. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Section: Maps/location services — device GPS/fused-location
         fix for centering the osmdroid map view on the user; ACCESS_COARSE
         is requested alongside FINE since Android may grant only the
         coarse permission depending on the user's choice in the
         system permission dialog, and LocationProvider.kt handles
         both gracefully rather than assuming FINE was granted. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".ArtificerXApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArtificerX"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        tools:targetApi="36">

        <!-- ArtificerXApp implements androidx.work.Configuration.Provider
             for on-demand WorkManager initialization (wiring HiltWorkerFactory
             in so background workers get Hilt injection). When that pattern
             is used, WorkManager's default eager initializer — normally
             auto-merged into the manifest via androidx.startup's
             InitializationProvider — must be explicitly removed, or lint's
             RemoveWorkManagerInitializer check fails release builds
             (WorkManager would otherwise try to initialize itself twice:
             once eagerly at app startup with default Configuration, and
             again on-demand with the Hilt-aware one). -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ArtificerX"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

            <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/artificerx_file_paths" />
        </provider>
    </application>
</manifest>
